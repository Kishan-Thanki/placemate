version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: placemate-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-placemate.settings.local}
    volumes:
      - .:/app
      - /app/venv
      - /app/__pycache__
      - /app/.pytest_cache
      - /app/staticfiles
      - /app/media
    command: python manage.py runserver 0.0.0.0:8000
    restart: unless-stopped
    networks:
      - placemate-network

networks:
  placemate-network:
    driver: bridge